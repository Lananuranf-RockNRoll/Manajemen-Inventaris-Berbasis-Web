# InvenSys Frontend — Environment Template
# Salin: cp .env.example .env
# JANGAN commit .env ke Git!

# URL base API — untuk dev lokal pakai /api (proxy via Vite ke backend :8000)
# Untuk production pakai /api (served under same domain via Nginx)
VITE_API_BASE_URL=/api
